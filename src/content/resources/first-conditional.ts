import type { Resource } from "@/lib/types";

/** Deep Dive — first conditional. Original wording (HANDOFF §9). */
export const firstConditional: Resource = {
  slug: "first-conditional",
  title: "First conditional, in depth",
  summary:
    "Talking about a real, likely future: if + present simple, will + base verb.",
  wiki: "english",
  section: "Sentence building",
  level: 3,
  tags: ["conditionals", "future"],
  related: ["present-perfect"],
  body: [
    "The **first conditional** talks about a **real and likely** future — a condition now, and the result that will probably follow.",
    "",
    "## The formula",
    "**If + present simple, will + base verb.**",
    "- *If it **rains** tomorrow, we **will stay** home.*",
    "- *I **will call** you if I **have** time.*",
    "",
    "Either half can come first; add a comma when the *if*-part leads.",
    "",
    "## The golden rule",
    "**No *will* in the *if*-part.** The condition stays in the present simple even though it's about the future:",
    "- ✓ *If it **rains**…*",
    "- ✗ *If it **will rain**…*",
    "",
    "## Swapping in other words",
    "- **unless** = *if not*: *I won't go **unless** you come.*",
    "- **as long as** = *only if*: *You can stay **as long as** you're quiet.*",
    "- **when** = it will definitely happen: *Call me **when** you arrive.*",
    "",
    "## Modals in the result",
    "Instead of *will*, use a **modal** for advice or possibility: *If you feel sick, you **should** rest.*",
  ].join("\n"),
};
